---
name: ntg062-applied-mods
description: NTG_062 — obiettivi utente e modifiche già applicate nell'APK consegnato (Audi A5 / italiano / alleggerito), con i TODO aperti
metadata:
  type: project
---

**Contesto utente**: possiede un'**Audi A5**, testata Android **NTG**, **senza root né chiave di piattaforma**; vuole l'app in **italiano** e **leggera/veloce**. Lavoro svolto il **2026-06-23**.

**Consegnato**: `NTG_062_audi_it_aligned.apk` (zipallineato, firma v1/v2/v3) in `/Users/alesimattia/Downloads/Android car/` — **3.9 MB** (da 10.5 MB). Prodotto anche un artifact-diagramma dello slider. (Le build intermedie e la cartella `NTG_062_extracted/` sono state rimosse perché ridondanti; gli asset originali restano comunque in `NTG_062.apk`.)

**Modifiche applicate** al progetto apktool (tutte verificate nell'APK finale):
1. `MyViewPageAdapter.smali` `getCount()`: `0x3 → 0x4` → abilita la pagina 3 = `CarKeyView` (4ª schermata, prima nascosta). Vedi [[ntg062-slider]].
2. `CarDoorWindowView.smali` `resetBenzCarDoorIcon`: inserito `const/4 v0, 0x2` subito dopo il `getInt` di `SETTING_BENZ_CAR_TYPE` → **fissa il tipo auto a 2** (Audi berlina/coupé ≈ A5).
3. Tenuti solo i 15 file `carinfo_audi_car_door_*_02`; eliminati l'intera famiglia benz + audi `_00/_01/_03` (**165 file, 6.49 MB**) e rimosse le relative righe da `public.xml`. Vedi [[ntg062-cardoor-images]].
4. Ripuntati i 7 `android:src` di `carinfo_benz_car_door_layout.xml` da benz non-tipizzato → audi `_02` (fix build, Trappola 4).
5. Tradotte 7 stringhe app in `res/values/strings.xml`: `app_name→Auto`, `app_name_navigation→Navigazione`, `carinfo_mileage→Chilometraggio`, `carinfo_speed→Velocità`, `enter→"Entra in"`, `distance_metre_after→"metri e poi"`, `distance_kilometer_after→"km e poi"`.
6. Eliminate tutte le `res/values-<lingua>` tranne `values-it` (**112 rimosse**) → forza l'italiano (fallback sul default tradotto).
7. Rimosso `sharedUserId` da `AndroidManifest.xml` (Strada C, vedi [[ntg062-overview]]).
8. Rimosso lo sfondo `car_info_bg` (un quasi-bianco su cui il testo bianco era illeggibile): tolto l'`<ImageView>` da `mileage_layout.xml` E `key_one_layout.xml`, eliminato `car_info_bg.png` + voce `public.xml`. Ora le schermate restano sul nero del ViewPager (`#f000`) senza creare alcuna immagine. Richiesto `rm -rf build/` per via della cache apktool (vedi [[ntg062-modding-build]]).
9. **Ottimizzazione velocità/peso (Fase 1)**: rimosso l'SDK **Tencent WeCar/TAES + gson** (~725 classi, ~5 MB di smali). `WeCarNaviFrameManager` ridotto a **stub** (4 metodi no-op: `<init>`/`setFrameLayout`/`initFrameManager`/`uninitFrameManager`); eliminati `smali/com/tencent`, `smali/com/google`, le inner-class `WeCarNaviFrameManager$*`, gli asset `cross_tip_*.png` (70) e `component_config*.json`. **`NaviManager`/`AmapAutoNaviFrameManager` NON toccati**: i loro presunti riferimenti a Tencent erano **costanti `static final` già inlined dal compilatore** (insight chiave: nello smali le final-constant NON sono ref di classe → l'unico vero referente di `com/tencent` era `WeCarNaviFrameManager`). AMap + pannello live intatti. + PNG ottimizzati con `optipng` (−151 KB). Vedi [[ntg062-dvr-tuning]] per la Fase 2.

10. **Rimozione COMPLETA anche di AMap** (l'utente non usa né WeCar né AMap, vuole Google Maps): `AmapAutoNaviFrameManager` ridotto a stub no-op (stesso pattern di WeCar), eliminati le inner-class `AmapAutoNaviFrameManager$*`, `GuideInfoExtraKey.smali` e i 29 `assets/a_map_auto_tip_*.png` (la cartella `assets/` ora è vuota). Ripuliti i placeholder di `navi_layout.xml` (" abce"/" efg"/"hhhhhh" → ""). Risultato: il pannello navi diventa un **riquadro statico** (sfondo + pin + "Navigazione") che al tap lancia `SETTING_NAVI_APP` (Google Maps) — **niente più indicazioni live** (erano alimentate da AMap). `NaviManager` NON toccato (chiama gli stub). Rimosse anche le 3 stringhe navi ora inutilizzate (`distance_metre_after`/`distance_kilometer_after`/`enter`) da `values/strings.xml`, `values-it/strings.xml` e `public.xml` (verificato: 0 usi reali per ID nello smali; `app_name_navigation` invece mantenuta perché usata dal layout). ⚠️ **STAGED in `NTG_062_src/`, NON ancora ricompilato** (su richiesta dell'utente).

APK consegnato finale (`NTG_062_audi_it_aligned.apk`): zipallineato + firma v1/v2/v3, **~3.3 MB** (da 10.5 MB). **NB**: questo APK include i punti 1–9 (WeCar rimosso) ma **NON ancora la rimozione AMap (punto 10)**, che è solo nei sorgenti finché non si ricompila (basta `./compile_sign_align.sh`, che fa pulizia cache + `apktool b` + firma).

11. **App resa installabile come pacchetto distinto + range SDK 8-11** (2026-06-24, staged in `NTG_062_src/`, NON ancora ricompilato). Problema: l'APK modificato condivideva `package="com.spd.xhsntg"` con l'app di sistema preinstallata ma con firma diversa (debug key) → Android tentava l'update, firma incompatibile → "App non installata". Fix nel solo `AndroidManifest.xml`: `package` → **`com.spd.xhsntg.audi`** (nuovo applicationId = app separata, niente conflitto di firma; complementa la Strada C del punto 7). Per non rompere lo smali (rimasto sotto `com/spd/xhsntg/`) i nomi componenti sono stati resi **assoluti**: `.FullscreenActivity`→`com.spd.xhsntg.FullscreenActivity`, `.ReverseBroadcast`→`com.spd.xhsntg.ReverseBroadcast` (altrimenti il package relativo punterebbe a classi inesistenti). I ref a `R` nello smali sono ID interi in package 0x7f → continuano a risolversi a prescindere dal nome package; `getPackageName()` (audio focus via SpdManager) ora ritorna il nuovo id, mentre il token sorgente hardcoded `com.spd.xhsntg|com.spd.ntg.ACTION` (enterSource) è stato lasciato invariato. In `apktool.yml`: `minSdkVersion 28→26` (consente install da Android 8.0); **targetSdkVersion lasciato a 28** di proposito — target non limita l'estremo superiore (gira su 8→11+) e tenerlo a 28 evita l'enforcement scoped-storage su Android 10/11 che romperebbe `WRITE_EXTERNAL_STORAGE`. Inoltre (sempre solo manifest): **etichetta** `android:label` → letterale **"Audi functions"** su `<application>` e sull'activity launcher (prima `@string/app_name`="Auto"; `app_name` lasciato "Auto", NON referenziato nello smali); **icona** `android:icon` da `@mipmap/ic_ntg_launcher` → `@android:mipmap/sym_def_app_icon` (icona default framework). I 4 file `res/mipmap-*/ic_ntg_launcher.png` + voce `public.xml` (id 0x7f080002) **conservati** → ripristino icona = ripuntare `android:icon` a `@mipmap/ic_ntg_launcher`. L'app modificata compare nel launcher (filtro MAIN/LAUNCHER già presente), l'originale di sistema no. ⚠️ da ricompilare per avere effetto.

13. **Restyle pagina km/velocita (indice 2, `CarMileageSpeedView`/`mileage_layout.xml`)** (2026-06-25, STAGED, NON ricompilato). Richiesta utente: niente icona km, niente etichette testuali, valori centrati. Fatto **solo nel layout** (`CarMileageSpeedView.smali` NON toccato: usa solo gli id `mileage`=0x7f050045 e `speed`=0x7f050050): rimossi `<ImageView src=@drawable/icon_mileage>` e i due `<TextView text=@string/carinfo_mileage|carinfo_speed>`; ogni riga ora e un `LinearLayout` orizzontale con `gravity="center"` dentro un contenitore verticale `width=match_parent` → **valore+unita centrati orizzontalmente** (riga 1 = solo `@id/mileage`; riga 2 = `icon_speed` + `@id/speed`). Heights riga: `match_parent`→`wrap_content`. **Icona velocita modernizzata**: `res/drawable/icon_speed.png` (80x80, bianco su trasparente) sostituita con uno speedometer piu' moderno disegnato con PIL — variante **V2 scelta dall'utente** (arco ~240 gradi + ~5 tacche brevi + lancetta diagonale + mozzo; piu' pulita dell'originale che aveva molte tacche). Originale recuperabile da git + `NTG_062_original.apk` (i file candidati temporanei V1/V2/confronto sono stati eliminati su richiesta). `icon_speed` mantiene id/nome → nessun cambio in `public.xml`/smali. Il valore mostrato include gia l'unita (`<n> KM|MILE`, `<v> KM/H|MPH`, vedi [[ntg062-slider]]). **Cleanup orfani eseguito (2026-06-25)**: rimossi il drawable `icon_mileage.png`, le stringhe `carinfo_mileage`/`carinfo_speed` (da `values/`+`values-it/`) e i 5 id `id_text_navi_cross_*` (orfani dopo la rimozione navi) — sia i file/stringhe sia le voci `public.xml`/`ids.xml`; verificato 0 usi reali (solo costanti `R$*` inerti, lasciate). Build ripulibile con `compile_sign_align.sh` (fa gia `rm -rf build/`).

12. **Rimozione pagina mappa/navi + aggiunta pagina nera iniziale** (2026-06-25, STAGED in `NTG_062_src/`, NON ancora ricompilato). L'utente prima voleva togliere "la prima pagina e la terza (mappa)", poi ha cambiato idea: **NON** togliere le porte, ma **aggiungere una pagina nera vuota come nuovo indice 0** e **togliere comunque la mappa**. Fatto: (a) **navi rimossa del tutto** — vedi dettaglio in [[ntg062-navigation]] (package `com/spd/navi`, `navi_layout.xml`, `navi_bg`/`navi_tip`, stringa `app_name_navigation`, voci `public.xml`); (b) **`MyViewPageAdapter` riscritto** ([[ntg062-slider]]): aggiunto campo `m_test_view_0` = `new android/view/View(context)` (pagina nera: nessuno sfondo → mostra il `#f000` del ViewPager, nessuna funzionalita/listener), `getCount` resta `0x6` (−navi +nera), packed-switch rimappato 0=nera/1=porte/2=km/3=comandi/4=debug/5=Sistema; `initManager`/`uninitManager` ora no-op. **Doors/CarInfoManager/Callback NON toccati** (la pagina porte resta pienamente funzionante). ⚠️ da ricompilare (`./compile_sign_align.sh`) per avere effetto.

14. **Debloat sicuro applicato** (2026-06-25, STAGED in `NTG_062_src/`, NON ricompilato): (a) `res/drawable/bg.png` (**1.5 MB**, sfondo del `LinearLayout` root di `activity_fullscreen.xml`, coperto da ViewPager nero + preview → mai visibile) → `android:background` ripuntato a `#ff000000`, PNG + voce `public.xml` (0x7f040001) eliminati; (b) `res/drawable/car.png` (113 KB, orfana) → eliminata + voce `public.xml` (0x7f040002); (c) **isola media morta** = `smali/androidx/media/` (434 classi) + `smali/android/support/v4/media/` (167 classi) = **601 classi, ~5.6 MB smali** — closure verificata chiusa (referenziate solo tra loro, mai da `com/spd` né altre lib live; manifest pulito) → rimossa per intero. Verificato 0 ref pendenti a `@drawable/bg`/`car` e a classi media. Orfani innocui lasciati: campi `R$drawable.bg/.car`, layout/id `notification_*`. Razionale completo in [[ntg062-debloat-targets]]. ⚠️ da ricompilare per avere effetto.

15. **Pagina indice 3 ricostruita: sensori/marcia al posto di CarKeyView** (2026-06-25, STAGED, NON ricompilato). I tasti di `CarKeyView` non funzionavano → classe **rimossa** (`smali/com/spd/car/CarKeyView.smali`, `res/layout/key_one_layout.xml`, voce `public.xml` del layout; lasciati orfani innocui `icon_03..icon_12` e i campi `R$id`/`R$layout`). Nuova **`com/spd/car/CarSensorView`** (smali a mano + blueprint `java/com/spd/car/CarSensorView.java`), stile **barre grafiche** (scelta utente): marcia + retromarcia (testo) e 4 righe `[etichetta | ProgressBar orizz. max 8 | valore]` per Anteriore/Posteriore/Sinistra/Destra. Dati in **PULL** da `CarInfo.ReverseAndAVM` (già registrata) via helper `pullInt` (pattern verificato di `CarInfoManager`: `instance().get(what,0,Integer)`), polling 300 ms con `Handler`+`Runnable` (`CarSensorView$1`) avviato/fermato da `FullscreenActivity$1.onPageSelected` (indice 3, come SystemView all'indice 5). what: GEAR 140080, REVERSE 140011, FRONT/REAR_RADAR_LEVEL 140007/140006 + LEFT/RIGHT 140008/140009 (barre), FRONT/REAR_RADAR_DISTANCE 140016/140017 (testo "cm"). `MyViewPageAdapter`: `m_test_view_3` ora `Landroid/view/View;`, creato via `CarSensorView.createView`, `setCarInfoManager` reso no-op. ✅ **BUILD ESEGUITA E VERIFICATA (2026-06-25, su PC-030 in deroga utente)**: `apktool b` ha assemblato lo smali a mano **senza errori** + zipalign + firma **v2/v3 verificata** → **`a:/aa/NTG_062_audi_it.apk` = 1.41 MiB** (da 10.5 MB originali). Questo valida a livello di build TUTTE le modifiche staged (punti 1-15). Procedura Windows in [[ntg062-sign-align-script]]. ⚠️ Resta da validare **SU TESTATA** (solo runtime, non build): encoding marcia (mostra valore grezzo), scala/disponibilità radar per l'Audi (barra `max=8` ipotetico); se un `what` non è popolato dal CAN-box il valore mostrato è **0** (non "--", perché `getInt` di un valore assente torna 0). NB CarInfo legge sotto Strada C (vedi [[ntg062-overview]]) → rischio "nessun dato" basso. Sorgente dati in [[ntg062-carinfo-data]].

16. **Rimozione pagina porte/finestrini (indice 1, `CarDoorWindowView`)** (2026-06-29, STAGED in `NTG_062_src/`, NON ricompilato). Su richiesta utente ("schermata 1 dello slider" = pagina porte, confermato). Slider sceso da 6 a **5 pagine** con rinumerazione indici 0-4 (la pagina nera resta indice 0): 0=nera, 1=km/velocita, 2=sensori, 3=debug, 4=Sistema. Fatto: (a) **`MyViewPageAdapter`** — eliminato campo+istanza `m_test_view_1`(CarDoorWindowView), rinumerati campi (km 2→1, sensori 3→2, debug 4→3, Sistema 5→4), `getCount 0x6→0x5`, packed-switch 0-4, **rimosso metodo `updateDoors(ZZZZZZ)V`**, `updateMileage`/`updateSpeed` ora puntano `m_test_view_1`; (b) **`FullscreenActivity$1.onPageSelected`** indici shiftati (debug 4→3, Sistema 5→4, sensori 3→2); (c) **`FullscreenActivity`** rimosso `onUpdateDoors`; (d) **`CarInfoManager`** rimosso il metodo privato `updateDoors`, il blocco pull porte (`what 0xc351`) in `onCarInfoConnectedChanged` (sostituito da solo `const/4 v3,0x0` perche v3=0 serve a speed/mileage/source successivi + ripuliti i `.end local` v0/v5-v10 penzolanti), il case `sswitch_4`(0xc351) in `onCarInfoDataChanged` + voce sparse-switch, e **tolto `CarInfo$Doors` dall'array di registrazione** in `init()` (5→4 elementi, reindicizzati: ReverseAndAVM/General/Instruments/Vehicles); (e) **`CarInfoManager$Callback`** rimosso `onUpdateDoors`; (f) **file eliminati**: `CarDoorWindowView.smali`+`$1`, `res/layout/carinfo_benz_car_door_layout.xml`, 15 PNG `res/drawable/carinfo_audi_car_door_*_02`; (g) **`public.xml`** rimosse 15 voci drawable + voce layout. **Lasciati orfani innocui**: 7 id `id_car_door_*` (ids.xml+public.xml, peso nullo, come prassi del progetto), campi costante in `R$drawable`/`R$id`, e la classe SDK `CarInfo$Doors` (NON rimossa: serve a `DebugLog$DumpTask` per il dump). NB la mod #2 (fix `SETTING_BENZ_CAR_TYPE=2`) era dentro `CarDoorWindowView` → sparisce con la classe. ⚠️ da ricompilare (`./compile_sign_align.sh`). Restore-page sicura: ViewPager clampa `setCurrentItem` a count-1 (vecchio "page"=5 → 4). Vedi [[ntg062-slider]] e [[ntg062-cardoor-images]].

17. **Rimozione pagina sensori/marcia (`CarSensorView`)** (2026-06-29, STAGED in `NTG_062_src/`, NON ricompilato). Motivo dichiarato dall'utente: **sulla sua Audi A5 nessuno dei dati mostrati funziona** — in particolare **GEAR (current gear, `140080`) e RADAR_DISTANCE (`140016`/`140017`) NON sono leggibili/popolati dal CAN-box** (anche i radar level 140006-140009 restavano a 0). Quindi la pagina che aveva sostituito CarKeyView (vedi punto 15) è stata a sua volta rimossa. ⚠️ **CHIARIMENTO**: l'utente l'ha chiamata "CarKeyView" ma quella classe non esiste più dal punto 15 — il bersaglio reale era `CarSensorView` (confermato). Slider sceso da 5 a **4 pagine**, rinumerati indici 0-3 (nera resta 0): 0=nera, 1=km/velocita, 2=debug, 3=Sistema. Fatto: (a) **`MyViewPageAdapter`** — eliminato campo+istanza `m_test_view_2`(CarSensorView via `createView`), rinumerati debug 3→2 e Sistema 4→3, `getCount 0x5→0x4`, packed-switch 0-3, `setCarInfoManager` resta no-op; (b) **`FullscreenActivity$1.onPageSelected`** — **rimosso il blocco start/stop CarSensorView**, shiftati debug 3→2 e Sistema 4→3; (c) **file eliminati**: `smali/com/spd/car/CarSensorView.smali`+`$1`. **NON toccato `CarInfoManager`**: `CarSensorView` leggeva in PULL da `CarInfo.instance()` (non dal manager), e la registrazione `ReverseAndAVM` **serve ancora alla velocita** (`140062`=`0x2231e`, usata dalla pagina km che resta) → registrazione invariata. `CarSensorView` non referenziava risorse app (solo stringhe inline + style framework `0x1010078`) → niente da pulire in `res/`/`public.xml`. Blueprint `java/com/spd/car/CarSensorView.java` lasciato come record storico (non compilato). ⚠️ da ricompilare. Vedi [[ntg062-slider]] e [[ntg062-carinfo-data]].

18. **Ottimizzazioni velocità avvio + commutazione retrocamera** (2026-06-30, STAGED in `NTG_062_src/`, NON ricompilato). Richiesta preventiva (nessun ritardo reale lamentato); uso tipico: retro inserita con app **in background** (es. Google Maps in primo piano). Analisi su smali di build (non jadx datato): avvio gia snello (no Application class, `windowAnimationStyle=@null`, no boot, niente piu decode/inflate pesanti — gia rimossi). Applicate 4 mod a basso rischio:
   - **Keep-bind DVR** (impatto maggiore nel caso d'uso): in `FullscreenActivity.onStop` **rimossi** `DvrHelper.unregsiterCallBack(this)` e l'azzeramento `m_service_connect_flag=0`; l'unregister e' **spostato in `onDestroy`**. Cosi il bind al servizio DVR resta vivo in background → al ritorno dalla retro si salta il round-trip `bindService` (~50-500 ms). Costo risorse ~nullo: la preview e' gia fermata da `onPause` (`setDvrChannel(0,0)`→`dvrControlCmd 1001`) quindi 0 decode in background; il servizio e' comunque tenuto vivo da `startService` in `initDvrHelper`. **Auto-healing verificato**: se il servizio muore in background, `DvrHelper$1.onServiceDisconnected` azzera `m_bind_flag`/`m_bind_request_flag`, notifica i callback (msg `0x66` → `FullscreenActivity.onServiceConnected`/`onServiceDisconnected` → `m_service_connect_flag`) e auto-rebinda. Restano NON eliminabili: ricreazione Surface + warm-up camera/encoder del servizio esterno (500-1500 ms), e l'incognita che la commutazione retro avvenga a livello hardware/LVDS nel servizio DVR a prescindere dall'app (da verificare con logcat).
   - **S1** (rischio nullo): rimossa la lettura ridondante di `SETTING_NTG_FULL_SCREEN` in `checkPreviewMode` (era usata **solo nel log**; il branching usa il campo `m_show_preview_mode` gia letto in `onCreate`). Una query Settings in meno all'avvio.
   - **S2** (no dialog/PM all'avvio): rimossa `requestPermissions(WRITE_EXTERNAL_STORAGE)` da `onCreate`; spostata **guardata** (`checkSelfPermission != GRANTED`) in `FullscreenActivity$1.onPageSelected` ramo indice **2** (pagina DebugLog), prima di `dumpAll()`. Scelta utente: il permesso si chiede solo quando si apre la pagina diagnostica. Il primo dump pre-concessione resta a schermo (DebugLog ha try/catch sul file). `.locals` di onPageSelected 2→4.
   - **S3** (ViewStub): `LvdsTestView` (overlay LVDS nascosto, mostrato solo da long-press 3s) non piu inflata in `setContentView`. In `activity_fullscreen.xml` sostituita con `<ViewStub id=ldvs_test_stub inflatedId=ldvs_test_layout layout=@layout/lvds_stub_content>`; nuovo file `res/layout/lvds_stub_content.xml` (contiene la `LvdsTestView`); nuovi id `ldvs_test_stub`=**0x7f05005c** (public.xml+ids.xml) e layout `lvds_stub_content`=**0x7f070015**. `FullscreenActivity$H.handleMessage` (msg `0x3e8`): se `findViewById(ldvs_test_layout)`==null infla lo stub (`((ViewStub)findViewById(0x7f05005c)).inflate()`), poi `setVisibility(VISIBLE)`; re-entrante (dopo l'inflate lo stub e' rimpiazzato). `.locals 3` invariato.
   - **S4 SCARTATO** (windowBackground): l'utente ha giustamente obiettato che non e' un knob di velocita (controlla solo il placeholder starting-window, fuori dal critical path). `styles.xml` lasciato invariato. ⚠️ da ricompilare (`compile_sign_align.sh`); poi validare su testata (vedi piano `cozy-riding-pizza.md` per la verifica + misura logcat opzionale). Vedi [[ntg062-dvr-tuning]] e [[ntg062-slider]].

19. **Round 2 ottimizzazioni** (2026-06-30, STAGED in `NTG_062_src/`, NON ricompilato — **da validare con `apktool b`**). 4 voci, ognuna con gate anti-regressione (piano `cozy-riding-pizza.md`):
   - **A1 — `SystemView` I/O termico pigro**: `buildTempRows` (enumera `/sys/class/thermal/thermal_zone0..9/{temp,type}`, ~20 letture sincrone) **tolta da `createView`** (girava in `onCreate` via il costruttore di `MyViewPageAdapter`, sul cold-start) e spostata in `start()` via nuovo `ensureTempRows()` (guardia `sTempRowsBuilt`, campo nuovo `sTable`). `updateTemps` gia null-guarda `sTempViews` → nessun crash se non costruite. **Rete di sicurezza** in `FullscreenActivity`: nuovo campo `m_view_pager` (salvato in onCreate), e `onResume` chiama `SystemView.start()` se `getCurrentItem()==3` (sana anche il bug latente "restore diretto su pagina Sistema senza onPageSelected" annotato in [[ntg062-system-page]]).
   - **PNG orfani**: rimossi `res/drawable/icon_03.png`..`icon_10.png` (8 file, id 0x7f0400be-c5) + voci `public.xml`. ⚠️ **NON** rimossi `icon_11/11_p/12/12_p/n/p`: verificato che sono usati dai drawable `carinfo_button_left/right/1_bg.xml` (a loro volta parte di `test_layout.xml`, VIVO perche inflato da `LvdsTestView`). Gli agenti li avevano erroneamente dati per orfani.
   - **Debloat androidx (solo dimensione, NON velocita)**: analisi di raggiungibilita SOUND (script `androidx_closure.py`/`androidx_reflcheck.py` in scratchpad): chiusura da `ViewPager`+`com/spd` = **solo 96 classi vive** su 1038; **942 irraggiungibili**. Reflection-check: 0 stringhe-classe rimovibili nelle classi vive (unica reflection viva = `ViewCompat.getDeclaredMethod` su metodi framework). **Rimossi i 17 moduli INTERAMENTE morti** (514 file smali): `android/support/v4` + androidx `arch/asynclayoutinflater/coordinatorlayout/cursoradapter/documentfile/drawerlayout/fragment/interpolator/legacy/lifecycle/loader/localbroadcastmanager/print/slidingpanelayout/swiperefreshlayout/versionedparcelable`. Lasciati i moduli PARZIALI (core 340/390 morte, viewpager 18/36, annotation 39/54, collection 7/17, customview 24/27): trimming deferito (piu rischioso, da validare su testata). Risorse `notification_*` lasciate (rimozione aapt2-sensibile). smali totali 1136→622. Verificato: 0 ref da `com/spd` e dal manifest ai moduli rimossi.
   - **Voce 4 — log su file `Download/NTG_062_log.log`** (richiesta utente; file DISTINTO dal logger can-box `ntg_carinfo_log.txt`; **sovrascritto a ogni avvio**): nuova classe **`com/spd/xhsntg/NtgLog`** (+`NtgLog$1` Handler) smali a mano (blueprint `java/com/spd/xhsntg/NtgLog.java`). API `d/e/i/w/v(tag,msg)` stessa firma di `android.util.Log` → richiama `Log` (logcat invariato) e accoda a un **HandlerThread "NtgLog"** che scrive con `BufferedWriter` (formato `HH:mm:ss LVL/tag: msg`). **Niente I/O sul chiamante** (percorsi caldi leggeri). **Tutto in try/catch(Throwable) → NtgLog non puo MAI far crashare l'app** (al peggio niente file). Pre-concessione permesso: bufferizza in memoria (cap 2000), flush all'apertura. **Permesso**: ripristinata `requestPermissions(WRITE_EXTERNAL_STORAGE)` in `onCreate` (**supera S2a**); rimossa la richiesta lazy in `FullscreenActivity$1.onPageSelected` (**S2b**); `NtgLog.init(appCtx)` come prima istruzione utile di onCreate; nuovo `onRequestPermissionsResult` → `NtgLog.flush()` alla concessione. **Redirect**: `sed` ha cambiato il target di **`Log.d`→`NtgLog.d`** in **14 file app** (`com/spd/xhsntg/*` + `com/spd/view/*`); SDK vendor `com/spd/carinfo` e `com/spd/dvr` **non toccati** (loro `Log.i/e` restano solo logcat). NtgLog stesso usa `Log.println` (no ricorsione).
   ✅ **BUILD ESEGUITA E VALIDATA (2026-06-30, su PC-030 con autorizzazione utente condizionata a "nessuna installazione" — toolchain gia presente: apktool in a:/tmp, Java 8 in PATH, JDK 12 per la firma)**: `apktool b` exit 0 → tutto lo smali a mano (NtgLog/NtgLog$1 inclusi) assembla, risorse OK; firma `[v2,v3]`+zipalign verificata con la chiave del repo. **`a:/aa/NTG_062_audi_it.apk` = 566 KB** (da 3.22 MiB del build precedente: −83%, soprattutto per il debloat androidx). Quindi l'assemblaggio di TUTTE le mod Round 1+2 e validato a livello di build. ⚠️ **RESTA il smoke test runtime su testata** (gate per il debloat androidx: un `NoClassDefFoundError` emerge solo a runtime; + verificare che `Download/NTG_062_log.log` si crei/popoli dopo la concessione permesso, e che la pagina Sistema mostri le temperature). Se regressione → ripristinare la voce coinvolta. Memorie correlate: [[ntg062-system-page]], [[ntg062-debloat-targets]].

**Sorgenti durevoli** (con tutte le modifiche già applicate) in `NTG_062_src/` nella root: edita lì e ricompila, niente più ri-decodifica. Workflow in [[ntg062-modding-build]] e `NTG_062_src/README.md`.

**TODO / aperto**:
- ✅ **Strada C confermata sulla testata utente (2026-06-25)**: telemetria `CarInfo` porte/velocità legge **dati reali** senza privilegi system (vedi [[ntg062-overview]]); la pagina Sistema gira. Resta da verificare solo per-`what` (quali dati il CAN-box Audi popola davvero) e le chiamate `SpdManager`/`Settings.System` singolarmente.
- Firma/allineamento: OK — ogni build è già **zipallineata + v1/v2/v3** via `./compile_sign_align.sh`.
- **Fase 2 ottimizzazioni DVR** (in attesa dell'APK `com.spd.dvr` dall'utente): mappare i valori validi e forzare qualità/fps migliori. Superficie di tuning in [[ntg062-dvr-tuning]].
- ✅ **B1 (bind DVR) RISOLTO meglio** col **keep-bind** del punto 18 (2026-06-30): invece di "anticipare" il bind si **mantiene vivo** in background → elimina il round-trip al ritorno dalla retro. A2 (rimozione UI debug LVDS) non fatto, ma S3 del punto 18 ne rende l'inflate **pigro** (ViewStub).
